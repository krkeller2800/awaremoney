# Codex Handoff

## Current Focus
- PDF credit-card import classification and reconciliation were stabilized after recent regressions.
- The Comenity/IKEA statement now balances after recovering a collapsed purchase amount from the transaction detail text.
- The Amazon/Synchrony statement now routes as a credit card instead of bank/checking/brokerage.
- The user manually confirmed Amazon balances and every other tested statement still balances.

## Important Constraints
- Do not use institution names in variable/helper names for general parser fixes.
- Avoid broad parser/classification changes without logs proving the failing layer.
- Watch for index/range safety in PDF text parsing paths.
- Preserve bank statement behavior; do not let weak credit-card signals override true bank summaries.

## Relevant Files Changed
- `DebtScope/Parsers/PDFBankTransactionsParser.swift`
  - Added a general recovery for collapsed credit-card transaction detail where a purchase amount is separated from the merchant line.
  - This fixed the Comenity/IKEA out-of-balance difference caused by a missing `$74.18` purchase.
- `DebtScope/Parsers/StatementIntakeClassifier.swift`
  - Resolver now handles the proven conflict `classifier=bank fallback=creditCard` by choosing `.creditCard`.
  - Existing opposite rule remains: a bank fallback can still override a weak credit-card classifier.

## Investigation Notes
- Earlier broader changes in `ImportViewModel.swift` and `StatementIntakeClassifier.swift` had no effect on Amazon and were rolled back.
- Logs showed the real Amazon failure path:
  - `classifier=bank`
  - `fallback=creditCard`
  - `final=bank`
- `PDFSummaryParser` was already detecting credit-card indicators and coercing parsed snapshot labels to credit card during fallback.
- The wrong final type came from resolver precedence, not transaction parsing or the import view-model guesser.

## Validation
- Live diagnostics were clean for touched files.
- Full Xcode project build succeeded after the resolver fix.
- User manually confirmed:
  - Amazon credit-card statement now identifies and balances.
  - Other tested statements still balance.

## Next Step
- If the current manual regression set remains clean, commit the import fixes.
- If another statement regresses, collect resolver/parser logs first before changing code.
